# WorkFlow 工作流

版本：`WF-1.0`  
时区：所有时间戳使用 UTC+8（Asia/Hong_Kong），格式 `YYYY-MM-DDTHH:mm:ss+08:00`。

## 目标

让每一次 Agent 执行都可追踪、可复现、可审查：

- 任务进入前先识别约束与项目范围。
- 任务执行前生成计划与任务 hash。
- 任务执行中遵守约束并逐步更新 TODO。
- 任务完成后写入 LONG_MEMORY 与 SHORT_MEMORY。
- 代码变更必须经过 CODE_REVIEW 门禁。

## 阶段总览

| 阶段 | 名称 | 主要产物 | 门禁 |
| --- | --- | --- | --- |
| WF-00 | 任务接收 | 规范化请求、项目范围 | 请求可执行且范围明确 |
| WF-01 | 上下文分析 | 约束清单、相关文件、影响面 | 上下文已覆盖全部相关文档 |
| WF-02 | 计划 | 任务 hash、执行计划、任务拆分 | 计划通过最小可执行校验 |
| WF-03 | 执行 | 代码/文档/配置变更 | 变更符合约束且不越界 |
| WF-04 | 验证 | 构建、测试、运行结果 | 无失败项，证据可复现 |
| WF-05 | 代码审查 | 审查意见、修复记录 | 无 P0/P1，P2 已闭环 |
| WF-06 | 记忆写入 | LONG_MEMORY、SHORT_MEMORY、TODO 更新 | 记录与事实一致 |
| WF-07 | 收尾汇报 | 结果说明、后续建议 | 用户已收到可验证结论 |

## 阶段说明

### WF-00 任务接收

输入：用户请求、当前工作目录、环境变量。

步骤：

1. 将请求整理为可执行语句。
2. 确定目标项目根目录：优先 `COCOS_AGENT_PROJECT_ROOT`，其次当前 Cocos 项目根目录，最后当前工作目录。
3. 检查 TODO.md 中是否已有相同或关联任务。

输出：`request`、`projectRoot`、`taskId`。

门禁：请求必须落在当前项目范围内；涉及范围外资源时先明确授权。

### WF-01 上下文分析

步骤：

1. 读取 `WORKFLOW.md`、`CONSTRAINTS.md`、`TODO.md`。
2. 按任务类型读取对应约束：Cocos Creator、TypeScript、JavaScript、项目级约束。
3. 检索相关文件与现有记忆（LONG_MEMORY、SHORT_MEMORY）。
4. 若任务涉及编辑器/Scene/素材，检查对应 Skill 与 MCP 工具是否可用。

输出：约束清单、影响文件列表、风险点。

### WF-02 计划

步骤：

1. 使用 HASH.md 规范计算任务 hash。
2. 将任务拆为可验证步骤，必要时使用 plan 工具跟踪。
3. 在 TODO.md 登记任务状态与负责人。

输出：`taskHash`、`plan`。

门禁：计划必须包含验证方式；纯文档任务也必须有检查清单。

### WF-03 执行

步骤：

1. 按计划逐项执行。
2. 每次修改文件前确认修改目的与影响范围。
3. 严格使用项目内编辑规则（apply_patch 等），不执行破坏性命令。
4. 只读写当前 Cocos 项目范围内的文件；越界读写必须显式报错。

输出：代码/文档/配置变更。

### WF-04 验证

验证类型按任务风险选择：

- 文档任务：检查链接、格式、约束覆盖。
- TypeScript：`tsc` 严格编译、lint、测试。
- CLI：构建后实际运行命令。
- Cocos 资源：在编辑器或 CLI 中校验 JSON 与路径。

输出：验证命令与结果。

门禁：存在失败项时必须先修复再进入下一阶段。

### WF-05 代码审查

按 `CODE_REVIEW.md` 执行：

- P0：阻断、安全/数据损坏/越界访问。
- P1：明显功能错误或规范违反。
- P2：可维护性问题，应闭环。
- P3：风格建议，可记录待办。

输出：审查结论、P0-P3 清单。

门禁：不允许携带 P0/P1 收尾；P2 需明确处理结果。

### WF-06 记忆写入

步骤：

1. 追加 LONG_MEMORY 条目：请求、推理、计划、时间线、任务 hash、结果。
2. 更新 SHORT_MEMORY，保留最近 10 次，最新在前。
3. 更新 TODO.md 状态与约束覆盖声明。

输出：已更新的记忆文件。

门禁：时间戳必须为 UTC+8；任务 hash 与 HASH.md 规范一致。

### WF-07 收尾汇报

步骤：

1. 汇总变更文件与验证结果。
2. 说明未完成项、风险、后续建议。
3. 如需 git 操作，报告分支、commit、PR 信息。

输出：最终答复。

## 重跑规则

- 任务中断后恢复：从 WF-01 重新确认上下文，再进入 WF-02。
- 同一任务多次执行：沿用同一任务语义，但每次执行生成独立记忆条目。
- 用户新消息与当前任务冲突时，以最新请求为准，重新进入 WF-00。

## 定义

- 任务：一次完整的用户请求执行单元。
- 项目：当前 Cocos 项目根目录。
- 执行：从 WF-00 到 WF-07 的一次完整流转。
- 门禁：进入下一阶段前必须满足的条件。
